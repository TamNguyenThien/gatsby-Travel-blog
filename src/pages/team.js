import React from 'react'
import Layout from '../components/layout'
import SEO from '../components/seo'

const TeamPage = () => (
  <Layout pageTitle='Out Team'>
    <SEO title='Team' keywords={[`gatsby`, `application`, `react`]} />
    <p>Info</p>
  </Layout>
)

export default TeamPage
